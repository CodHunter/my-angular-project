export class MyFile {
    fileName: string;
    fileType: string;
    size: string;

    constructor(fileName, fileType, size) {
        this.fileName = fileName;
        this.fileType = fileType;
        this.size = size;
    }
}
